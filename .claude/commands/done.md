Review what changed in this session and update docs. Check each item:

1. **CHANGELOG.md** — add entry for the change
2. **FILE-TREE.md** — update if files/folders were added or removed
3. **CLAUDE.md sub-files** — update if architecture or conventions changed
4. **backend/users/README.md** or **backend/expenses/README.md** — update if API changed
5. **docs/issues/** — close fixed issues, update ongoing ones
   - For each item marked resolved in `docs/issues/ongoing/fixes-and-suggestions.md`: move it to `docs/issues/fixed/fixes-and-suggestions-applied.md` (create the file if it does not exist)
   - For each QA item resolved in `docs/issues/ongoing/qa_test_results/`: move it to the matching file under `docs/issues/fixed/qa/` (create the file if it does not exist)

Report which files you updated and which you skipped (with reason).
