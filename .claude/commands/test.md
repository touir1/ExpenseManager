Run tests for the current context. Detect scope from recent files or $ARGUMENTS.

- Frontend: `cd frontend/dashboard && npm test`
- Expenses backend: `cd backend/expenses && dotnet test`
- Users backend: `cd backend/users && dotnet test`
- All: run all three above

Report: pass/fail counts and any failures with file:line. Fix failures if asked.
