title = "ExpenseManager gitleaks configuration"

[allowlist]
description = "Ignore known false positives in config imports"
# Ignore password hashes in RabbitMQ management definitions (hashed, not secrets)
paths = ["infrastructure/configs/rabbitmq/management_definitions.json"]
regexes = ['"password_hash"\s*:\s*".*"']

