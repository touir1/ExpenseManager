# Gitlab CI template for Docker image security scanning

.docker-security:
  stage: docker-security
  image: 
    name: ghcr.io/aquasecurity/trivy:latest
    entrypoint: [""]
  services:
    - name: docker:24-dind
      command: ["--host=tcp://0.0.0.0:2375", "--insecure-registry=gitlab:5050", "--insecure-registry=172.50.0.10:5050"]
  variables:
    DOCKER_TLS_CERTDIR: ""
    DOCKER_HOST: tcp://docker:2375
    TRIVY_USERNAME: "gitlab-ci-token"
    TRIVY_PASSWORD: "$CI_JOB_TOKEN"
    TRIVY_AUTH_URL: "$CI_REGISTRY"
    TRIVY_NO_PROGRESS: "true"
    TRIVY_CACHE_DIR: ".trivycache/"
    TRIVY_INSECURE: "true"
    TRIVY_NON_SSL: "true"
  script:
    - |
      if [ -z "$IMAGE_NAME" ]; then
        echo "IMAGE_NAME variable is not set. Please set it in the CI configuration."
        exit 1
      fi
    - FULL_IMAGE_NAME="$CI_REGISTRY_IMAGE/$IMAGE_NAME:latest"
    - echo "Scanning image $FULL_IMAGE_NAME"
    - trivy --version
    # update vulnerabilities db
    - time trivy image --download-db-only
    # Builds report and puts it in the default workdir $CI_PROJECT_DIR, so `artifacts:` can take it from there
    - time trivy image --exit-code 0 --format template --template "@/contrib/gitlab.tpl" --output "gl-container-scanning-report.json" --ignorefile .trivyignore "$FULL_IMAGE_NAME"
    # Prints full report
    - time trivy image --exit-code 0 --ignorefile .trivyignore "$FULL_IMAGE_NAME"
    # Fail on high vulnerabilities
    - time trivy image --exit-code 1 --severity HIGH --ignorefile .trivyignore "$FULL_IMAGE_NAME"
  cache:
    paths:
      - .trivycache/
  artifacts:
    when: always
    paths:
      - $BASE_DIR_PATH/gl-container-scanning-report.json